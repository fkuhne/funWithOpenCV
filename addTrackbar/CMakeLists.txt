cmake_minimum_required(VERSION 2.8)
project( addTracker )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( addTracker main.cpp )
target_link_libraries( addTracker ${OpenCV_LIBS} )
